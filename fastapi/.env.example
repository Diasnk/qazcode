# Required for the diagnose server (LLM)
OPENAI_API_KEY=sk-your-openai-key

# Optional
OPENAI_MODEL=gpt-4.1
# EMBEDDING_BASE_URL is set in docker-compose for the app container
EMBEDDING_MODEL=ai-forever/ru-en-RoSBERTa

# Set to 1 to build Chroma from extracted_data/*.json if DB is missing
BUILD_CHROMA_IF_MISSING=1

# LOG_LEVEL=DEBUG
